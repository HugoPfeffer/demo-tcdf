[x] Criação de cluster;
_ Use `openshift-install` binary with `AWS Blank Enviroment`
[x] Adição e remoção de nós;
_ Change `MachineSets`
[x] Atualização;
_ Update a throw-away cluster since it takes too long for a demo
[x] Dashboards para verificação de saúde dos nós e dos pods;
_ Use native `Graphana` e `Prometheus`/`Thanos` queries
[x] Dashboards para verificação de consumo dos pods;
_ Use native `Graphana` e `Prometheus`/`Thanos` queries
[x] Rede e Ingress (configuração e visibilidade de tráfego e deploy de loadbalancer);
_ No need to deploy `LoadBalancer`
_ Use native `Routes` for showcasing exposing applications to the web
[] Backup e restauração;
_ Use `Velero Operator`
_ Needs exploring
[] Integração com Zabbix;
_ Needs finishing up a demo
[] Provisionamento de volumes (StorageClass apontando para um servidor NFS ou um target iSCSI, por exemplo); \_ Needs further preparation
